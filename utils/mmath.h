// find g.c.d of two integers
int findGCD(int a,int b);

// find abs of integer abs(x) = (x if x>0) (-x is x<0)
int mabs(int n);

// find lcm of two integers lcm(a,b) = |ab| / gcd(a,b)
int mlcm(int a, int b);